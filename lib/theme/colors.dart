import 'package:flutter/material.dart';

class xColors {
  static Color black =
      const Color.fromRGBO(18, 18, 18, 1.0);
  static Color white = Colors.white;
  static Color red = Colors.red;
  static Color grey =
      const Color.fromRGBO(34, 34, 34, 1.0);

  static Color orange =
      const Color.fromRGBO(85, 65, 21, 1.0);
}
